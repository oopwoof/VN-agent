# Docker environment — copy to .env.docker and customize
# Mock mode: runs without any API key (uses canned responses)
VN_AGENT_MOCK=true

# Uncomment and set these to use real LLM APIs:
# VN_AGENT_MOCK=false
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
